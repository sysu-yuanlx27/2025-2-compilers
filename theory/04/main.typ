#import "@local/sysu-assignment:0.1.0": *

#show: assignment.with(
  title: "编译原理理论作业",
  subtitle: "第四章作业（二）",
  student: (name: "元朗曦", id: "23336294"),
  lang: "zh",
)

#problem[
  证明文法 $G[S]$：

  $
    S &-> C a | b C e | D e | b D a \
    C &-> d \
    D &-> d
  $

  是 $"LR"(1)$ 而不是 $"LALR"(1)$ 的，构造 $"LR"(1)$ 分析表，并对输入字符串 b d a \$ 进行 $"LR"(1)$ 分析，写出分析的全过程（表格形式）。
][
  设增广文法为 $S' -> S$，并将产生式编号如下：

  (1) $S -> C a$
  (2) $S -> b C e$
  (3) $S -> D e$
  (4) $S -> b D a$
  (5) $C -> d$
  (6) $D -> d$

  *1. 规范 LR(1) 项目集族*

  $
    I_0: S' &-> dot S, \
      S &-> dot C a, \
      S &-> dot b C e, \
      S &-> dot D e, \
      S &-> dot b D a, \
      C &-> dot d, \
      
  $

  I_0 = {S' -> .S, \$; S -> .C a, \$; S -> .b C e, \$; S -> .D e, \$; S -> .b D a, \$; C -> .d, a; D -> .d, e}

  I_1 = {S' -> S., \$}

  I_2 = {S -> C.a, \$}

  I_3 = {S -> b.C e, \$; S -> b.D a, \$; C -> .d, e; D -> .d, a}

  I_4 = {S -> C a., \$}

  I_5 = {S -> b C.e, \$}

  I_6 = {S -> b D.a, \$}

  I_7 = {C -> d., a; D -> d., e}

  I_8 = {S -> b C e., \$}

  I_9 = {S -> b D a., \$}

  I_10 = {C -> d., e; D -> d., a}

  其中，I_7 与 I_10 的 LR(0) 核相同，都是 {C -> d., D -> d.}，但展望符分别为 {a, e} 和 {e, a}。在规范 LR(1) 项目集族中它们是两个不同状态，所以不会冲突；若按 LALR(1) 合并这两个状态，则两个归约项目都会带上 a 和 e，从而在同一输入符号上出现归约-归约冲突，因此该文法不是 $"LALR"(1)$。

  *2. LR(1) 分析表*

  #table(
    columns: 6,
    inset: 4pt,
    stroke: 0.5pt,
  )[
      [状态], [a], [b], [d], [e], [\$]
    [0], [-], [s3], [s7], [-], [-]
    [1], [-], [-], [-], [-], [acc]
    [2], [s4], [-], [-], [-], [-]
    [3], [-], [-], [s10], [-], [-]
    [4], [-], [-], [-], [-], [r1]
    [5], [-], [-], [-], [s8], [-]
    [6], [s9], [-], [-], [-], [-]
    [7], [r5], [-], [-], [r6], [-]
    [8], [-], [-], [-], [-], [r2]
    [9], [-], [-], [-], [-], [r4]
    [10], [r6], [-], [-], [r5], [-]
  ]

  #table(
    columns: 4,
    inset: 4pt,
    stroke: 0.5pt,
  )[
    [状态], [S], [C], [D]
    [0], [1], [2], [-]
    [3], [-], [5], [6]
  ]

  其余 GOTO 项均为空。

    *3. 对输入串 b d a \$ 的分析过程*

  #table(
    columns: 5,
    inset: 4pt,
    stroke: 0.5pt,
  )[
    [步骤], [状态栈], [符号栈], [输入串], [动作]
    [1], [0], [-], [bda\$], [s3]
    [2], [0 3], [b], [da\$], [s10]
    [3], [0 3 10], [b d], [a\$], [r6: D -> d, goto(3, D) = 6]
    [4], [0 3 6], [b D], [a\$], [s9]
    [5], [0 3 6 9], [b D a], [\$], [r4: S -> bDa, goto(0, S) = 1]
    [6], [0 1], [S], [\$], [acc]
  ]
]